export const MovementConfig = {
  // Speed
  walkSpeed: 2.5,
  runSpeed: 6,
  runDistanceThreshold: 5,

  // Acceleration
  acceleration: 8,
  deceleration: 10,

  // Rotation
  turnSpeed: 8,

  // Stopping
  stoppingDistance: 1.5,

  // Offset from cursor
  cursorOffset: 2,

  // Smoothing
  positionSmoothing: 0.1,
  rotationSmoothing: 0.15,
};

export const FlightConfig = {
  flightDistanceThreshold: 15,
  flySpeed: 9,
  flightHeight: 1.6,
  landingDistance: 1.5,
  climbSpeed: 2.5,
  turnSpeed: 3.2,
  acceleration: 7.5,
  deceleration: 4.5,
};
